from setuptools import setup, find_packages

setup(
    name='testfmripack',
    version='1.0',
    packages=find_packages(),
    install_requires=[
        # List dependencies here if you have any
    ]
)
